<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<include resource="org/springframework/boot/logging/logback/defaults.xml"/>

	<springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <springProperty scope="context" name="loggerFolder" source="logger.folder"/>
	<!-- Example for logging into the build folder of your project -->
	<property name="LOG_FILE" value="${loggerFolder}/${springAppName}/${springAppName}.log"/>

	<!-- You can override this to have a custom pattern -->
	<property name="CONSOLE_LOG_PATTERN"
    	value="[%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint}] [%clr(%5p)] [${springAppName},%X{X-B3-TraceId:-},%X{X-B3-SpanId:-},%X{X-Span-Export:-}] [%clr(${PID:- }){magenta}] %clr([%15.15t]){faint} [%clr(%class{39}.%M\\(%line\\)){cyan}] %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
	<!-- Appender to log to console -->
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<!-- Minimum logging level to be presented in the console logs-->
			<level>INFO</level>
		</filter>
		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
			<charset>utf8</charset>
		</encoder>
	</appender>

	<!-- Appender to log to file -->
	<appender name="flatfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_FILE}</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.gz</fileNamePattern>
			<maxHistory>7</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>${CONSOLE_LOG_PATTERN}</pattern>
			<charset>utf8</charset>
		</encoder>
	</appender>

	<logger name="org.springframework.boot.actuate.endpoint.jmx" level="warn"/>
	<Logger name="com.netflix.discovery.shared.resolver.aws.ConfigClusterResolver" level="warn"/>
	<!--sql日志-->
	<logger name="com.rome.promotion.core.infrastructure.mapper" level="DEBUG"/>
	<!--无用debug日志过滤-->
	<logger name="org.apache.kafka" level="INFO"/>
	<logger name="com.netflix.discovery" level="INFO"/>
	<logger name="com.zaxxer.hikari.pool" level="INFO"/>
	<logger name="c.n" level="INFO"/>
	<logger name="o.a" level="INFO"/>


	<!--由于springboot默认使用的就是logback日志管理，所以只要引入：spring-boot-starter-web-->
   <!--日志级别error>warn>info>debug-->
    <root level="DEBUG">
		<!--输出到控制台-->
        <appender-ref ref="console"/>
        <!-- 输出到文件中 -->
        <!-- <appender-ref ref="flatfile"/>-->
    </root>
    

</configuration>